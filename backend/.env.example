# Backend Environment Variables

# Database
DATABASE_URL=sqlite:///./movemaster.db
# For PostgreSQL: postgresql://user:password@localhost/movemaster

# Supabase (optional - for production storage)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# Google Maps API (optional - for distance calculation)
# Get your key at: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Security
SECRET_KEY=your-secret-key-change-in-production

# Email (optional - for quote notifications)
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# CORS (comma-separated allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Environment
ENV=development
# ENV=production
